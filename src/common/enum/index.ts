export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum UserRole {
  MANAGER = `manager`,
  MEMBER = `member`,
}

export enum AppealStatus {
  ACTIVE = 'active',
  CLOSED = 'closed',
}
